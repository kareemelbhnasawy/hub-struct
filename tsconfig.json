{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    "jsx": "react-jsx", // fixes the 'React' refers to a UMD global, but the current file is a module. Consider adding an import instead.ts(2686)
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
      // "@assets": ["src/assets/*"],
      // "@components": ["src/components/*"]
      // "@constants": ["src/constants/*"],
      // "@hooks": ["src/hooks/*"],
      // "@localization": ["src/localization/*"],
      // "@navigation": ["src/navigation/*"],
      // "@services": ["src/services/*"],
      // "@store": ["src/store/*"],
      // "@style": ["src/style/*"],
      // "@theme": ["src/theme/*"],
      // "@utilities": ["src/utilities/*"]
    }
  }
}
