import { createThemedStyles } from '@/utilities';

const styles = createThemedStyles({
  container: {
    base: {
      alignItems: 'center',
      width: '100%',
    },
  },
  avatar: {
    base: {
      marginTop: 'auto',
    },
  },
  titleWrapper: {
    base: {
      alignItems: 'center',
    },
  },
});

export default styles;

