// handle connection for API requests
// eslint-disable-next-line @typescript-eslint/no-explicit-any
const connectionInterceptor = (config: any): any =>
  // implement connection interceptor
  config;
export default connectionInterceptor;
