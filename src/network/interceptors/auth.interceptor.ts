// handle authentication for API requests
// eslint-disable-next-line @typescript-eslint/no-explicit-any
const authInterceptor = (config: any): any =>
  // implement adding auth to header
  config;
export default authInterceptor;
