import { createThemedStyles } from '@/utilities';

const styles = createThemedStyles({
  container: {
    base: {
      borderRadius: 999999,
      width: 44,
      height: 44,
    },
  },
});

export default styles;
