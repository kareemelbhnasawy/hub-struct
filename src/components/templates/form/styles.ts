import { createThemedStyles } from '@/utilities';

const styles = createThemedStyles({
  flexOne: { base: { flex: 1 } },
  container: { base: { flex: 1, marginBottom: 10 } },
});

export default styles;
