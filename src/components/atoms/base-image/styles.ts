import { createThemedStyles } from '@/utilities';

export const styles = createThemedStyles({
  fullSize: {
    base: {
      height: '100%',
      width: '100%',

    }
  },
  roundedFull: {
    base: {
      borderRadius: 9999,
    }
  },
});
